#include <stdio.h>
int main() {
   
    char str[6] = "Hello";

    /*
    in order to print variable. Need to use format specifier first, followed by variable name
    print formating is done inside of quotation marks
    */
    printf("%s\n", str);


    /*
    prints str variable 10 times using a for loop
    */
    // for(int n = 0; n < 10; ++n) {
    //     printf("%s\n", str);
    // };

}